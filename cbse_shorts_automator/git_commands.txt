# 1. Stage all changes
git add .

# 2. Commit the staged changes with a message
git commit -m "changed woff2 to woff fonts and fixed texture error for cloud"

# 3. Push the new commit to the remote GitHub repository
git push